<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <title>Tombstone 4x6 Portrait</title>
    <style>
        @page { size: 11in 17in; margin: 0.25in; } /* Tabloid portrait */
        html, body { height: 100%; }
        html, body, * { box-sizing: border-box; }
        body {
            margin: 0;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
            font-feature-settings: "liga" 1, "kern" 1;
            font-kerning: normal;
            display: grid;
            grid-template-columns: repeat(2, 1fr); /* 2 columns for 4x6 */
            grid-template-rows: repeat(3, 1fr);    /* 3 rows for 4x6 */
            gap: 0.1in;
            padding: 0.25in;
        }
        :root {
            --serif: Georgia, "Times New Roman", Times, serif;
            --sans: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            --muted: #666;
            --ink: #111;
            --border: #ccc;
            --light-border: #e0e0e0;
        }
        .card-64 {
            width: 5in;     /* 4" + 1" for bleed/margin */
            height: 7in;    /* 6" + 1" for bleed/margin */
            max-width: 5in;
            max-height: 7in;
            padding: 0.2in;
            color: var(--ink);
            display: flex;
            flex-direction: column;
            overflow: hidden;
            border: 1pt solid #000; /* cut guide */
            background: white;
        }
        .header-section {
            display: flex;
            flex-direction: column;
            gap: 3pt;
            flex-grow: 1;
        }
        .title-price-row {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            gap: 12pt;
            margin-bottom: 4pt;
        }
        .title {
            font-family: var(--serif);
            font-weight: 600;
            font-size: 22pt;  /* Increased from 16pt */
            line-height: 1.1;
            hyphens: auto;
            margin: 0;
            font-style: italic;
            flex: 1;
        }
        .price {
            font-family: var(--serif);
            font-size: 18pt;  /* Increased from 14pt */
            font-weight: 600;
            color: var(--ink);
            margin: 0;
            text-align: right;
            flex-shrink: 0;
        }
        .artist-info {
            font-family: var(--sans);
            font-size: 16pt;  /* Increased from 13pt */
            line-height: 1.2;
            margin: 0 0 8pt 0;
        }
        .artist-name {
            font-weight: 600;
        }
        .artist-tagline {
            font-family: var(--sans);
            font-size: 12pt;
            line-height: 1.3;
            color: var(--muted);
            font-style: italic;
            margin: 4pt 0 0 0;
        }
        .meta {
            font-family: var(--sans);
            font-size: 12pt;  /* Increased from 10pt */
            line-height: 1.4;
            display: flex;
            flex-direction: column;
            gap: 3pt;
        }
        .meta-row {
            display: flex;
            align-items: baseline;
            break-inside: avoid;
        }
        .meta-icon {
            margin-right: 6pt;
            font-size: 12pt;
        }
        .meta-value {
            flex: 1;
        }
        .foot {
            margin-top: auto;
            padding-top: 8pt;
            border-top: 1pt solid var(--border);
            font-family: var(--sans);
            font-size: 9pt;   /* Increased from 8pt */
            flex-shrink: 0;
        }
        .footer-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 8pt;
            width: 100%;
            margin-bottom: 6pt;
        }
        .footer-left {
            display: flex;
            align-items: center;
            flex: 0 0 auto;
        }
        .footer-right {
            display: flex;
            align-items: center;
            gap: 8pt;
            margin-left: auto;
        }
        .qr-section {
            display: flex;
            align-items: center;
            gap: 4pt;
            height: 100%;
        }
        .headphone-icon {
            font-size: 11pt;
            color: var(--muted);
        }
        .chijal-logo {
            height: 50pt;  /* Increased from 42pt */
            width: auto;
        }
        .qr-code {
            width: 40pt;   /* Increased from 32pt */
            height: 40pt;
            background: white;
        }
        .artwork-image {
            height: 40pt;  /* Increased from 32pt */
            max-width: 75pt;
            object-fit: cover;
            border-radius: 4pt;
            border: 1pt solid var(--border);
            background: #f8f8f8;
        }
        .artwork-placeholder {
            height: 40pt;
            width: 40pt;
            background: #f8f8f8;
            border: 1pt dashed var(--border);
            border-radius: 4pt;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--muted);
            font-size: 7pt;
            text-align: center;
        }

        /* New contact and payment info section */
        .contact-info {
            text-align: center;
            font-family: var(--sans);
            font-size: 9pt;
            color: var(--muted);
            padding: 4pt 0;
            border-top: 0.5pt solid var(--light-border);
            line-height: 1.3;
            margin-top: 4pt;
        }
        .contact-row {
            margin: 2pt 0;
        }
        .payment-info {
            font-weight: 500;
            color: var(--ink);
        }

        .exhibition-name {
            text-align: center;
            font-family: var(--sans);
            font-size: 8pt;   /* Increased from 7pt */
            color: var(--muted);
            padding-top: 4pt;
            border-top: 0.5pt solid var(--border);
            width: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 6pt;
        }
        .tagline-logo {
            height: 40pt;  /* Increased from 36pt */
            width: auto;
        }
        .code {
            letter-spacing: 0.2pt;
            font-variant-numeric: tabular-nums;
            font-family: 'Monaco', 'Menlo', monospace;
            font-size: 9pt;
        }
        .muted { color: var(--muted); }

        @media print {
            body { margin: 0; padding: 0.25in; }
            .card-64 { border: 1pt solid #000; break-inside: avoid; }
        }
    </style>
</head>
<body>
{% for idx, obra in obras %}
    <main class="card-64">
        <section class="header-section">
            <div class="title-price-row">
                {% if obra.title %}
                    <h1 class="title">{{ obra.title }}</h1>
                {% endif %}
                {% if obra.price %}
                    <div class="price">${{ obra.price|number_format }}</div>
                {% endif %}
            </div>

            <div class="artist-info">
                {% if obra.artist and obra.artist.name %}
                    <span class="artist-name">{{ obra.artist.name }}</span>
                {% else %}
                    <span class="artist-name">Artista desconocido</span>
                {% endif %}
                {% if obra.year %} · {{ obra.year }}{% endif %}

                {# Optional artist tagline - uncomment if you have this data #}
                {% if obra.artist and obra.artist.tagline %}
                    <div class="artist-tagline">{{ obra.artist.tagline }}</div>
                {% endif %}
            </div>

            <div class="meta">
                {% if obra.materials %}
                    <div class="meta-row">
                        <span class="meta-icon">🎨</span>
                        <span class="meta-value">{{ obra.materials }}</span>
                    </div>
                {% endif %}

                {% if obra.size %}
                    <div class="meta-row">
                        <span class="meta-icon">📏</span>
                        <span class="meta-value">{{ obra.size }}</span>
                    </div>
                {% endif %}
            </div>
        </section>

        <footer class="foot">
            <div class="footer-content">
                <div class="footer-left">
                    {% set logoUrl = asset('/chijal-landscape.jpg') %}
                    <a href="{{ logoUrl }}">
                        <img src="{{ logoUrl }}" alt="Chijal" class="chijal-logo" />
                    </a>
                </div>
                <div class="footer-right">
                    <div class="qr-section">
                        <span class="headphone-icon">🎧</span>
                        {% set qrUrl = voxitourEndpoint ~ 'es/chijal?obraId=' ~ obra.code ~ '#tab-obras' %}
                        <a href="{{ qrUrl }}" target="_blank">
                            <img src="{{ qr_code_url(qrUrl) }}" alt="QR Code" class="qr-code" />
                        </a>
                    </div>

                    {% set primaryImage = obra.image %}
                    {% if primaryImage and primaryImage.resized %}
                        <img src="{{ primaryImage.resized.large }}"
                             alt="{{ obra.title }}"
                             class="artwork-image" />
                    {% else %}
                        <div class="artwork-placeholder">
                            Sin imagen
                        </div>
                    {% endif %}
                </div>
            </div>

            {# New contact and payment information section #}
            <div class="contact-info">
                <div class="contact-row">
                    <strong>Chijal</strong> • contacto@chijal.com • +52 967 123 4567
                </div>
                <div class="contact-row payment-info">
                    💳 Aceptamos tarjetas de crédito y débito
                </div>
            </div>

            <div class="exhibition-name">
                <span>{{ app.request.get('tagline', 'Ruta Chijal • San Cristóbal de las Casas, 2025') }}{% if app.environment == 'dev' %} • {{ obra.code }}{% endif %}</span>
            </div>
        </footer>
    </main>
{% endfor %}
</body>
</html>