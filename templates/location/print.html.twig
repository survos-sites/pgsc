<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <title>Location Card 6x4</title>
    <style>
        @page { size: 6in 4in; margin: 0; }
        html, body { height: 100%; }
        html, body, * { box-sizing: border-box; }
        body {
            margin: 0;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
            font-feature-settings: "liga" 1, "kern" 1;
            font-kerning: normal;
        }
        :root {
            --serif: Georgia, "Times New Roman", Times, serif;
            --sans: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            --muted: #555;
            --ink: #111;
        }
        .card-64 {
            width: 6in;
            height: 4in;
            padding: 0.2in;
            color: var(--ink);
            display: grid;
            grid-template-rows: auto 1fr auto;
            gap: 6pt;
            overflow: hidden;
            page-break-after: always;
            border: 1pt solid #000; /* cut guide */
        }
        .title {
            font-family: var(--serif);
            font-weight: 600;
            font-size: 18pt;
            line-height: 1.2;
            hyphens: auto;
            margin: 0;
        }
        .subtitle {
            margin-top: 2pt;
            font-family: var(--sans);
            font-size: 11pt;
            line-height: 1.25;
        }
        .italic { font-style: italic; }
        .muted { color: var(--muted); }
        .meta {
            margin-top: 6pt;
            font-family: var(--sans);
            font-size: 10pt;
            line-height: 1.3;
        }
        .meta .row { display: block; break-inside: avoid; }
        .meta .label { display: inline-block; min-width: 82pt; color: var(--muted); }
        .obras-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(120pt, 1fr));
            gap: 8pt;
            margin-top: 8pt;
        }
        .obra-item {
            break-inside: avoid;
            page-break-inside: avoid;
        }
        .obra-image {
            width: 100%;
            height: 70pt;
            object-fit: cover;
            border: 0.5pt solid #ddd;
            border-radius: 2pt;
            margin-bottom: 3pt;
        }
        .obra-title {
            font-size: 9pt;
            font-weight: 600;
            line-height: 1.2;
            margin-bottom: 2pt;
        }
        .obra-artist {
            font-size: 8pt;
            color: var(--muted);
            font-style: italic;
        }
        .no-image {
            width: 100%;
            height: 70pt;
            background: #f8f8f8;
            border: 1pt dashed #ccc;
            border-radius: 2pt;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 7pt;
            color: var(--muted);
            margin-bottom: 3pt;
        }
        .foot {
            margin-top: 6pt;
            padding-top: 6pt;
            border-top: 0.5pt solid #bbb;
            font-family: var(--sans);
            font-size: 9pt;
            display: flex;
            gap: 8pt;
            align-items: center;
            justify-content: space-between;
        }
        .code { letter-spacing: 0.2pt; font-variant-numeric: tabular-nums; }
        @media print { body { margin: 0; } }
    </style>
</head>
<body>
<main class="card-64">
    <section>
        {% if location.name %}
            <h1 class="title">{{ location.name }}</h1>
        {% endif %}
        <div class="subtitle">
            <strong>Ubicación</strong>
            {% if location.type %} · {{ location.typeString }}{% endif %}
        </div>
        <div class="meta">
            {% if location.obras|length %}
                <span class="row"><span class="label">Obras</span> {{ location.obras|length }} obra{{ location.obras|length != 1 ? 's' : '' }}</span>
            {% endif %}
            {% if location.lat and location.lng %}
                <span class="row"><span class="label">Coordenadas</span> {{ location.lat }}, {{ location.lng }}</span>
            {% endif %}
            {% if location.address %}
                <span class="row"><span class="label">Dirección</span> {{ location.address }}</span>
            {% endif %}
        </div>
    </section>
    <section class="obras-content">
        {% if obras|length %}
            <div class="obras-grid">
                {% for obra in obras %}
                    <div class="obra-item">
                        {% set primaryImageCode = obra.primaryImageCode %}
                        {% set primaryImage = primaryImageCode ? imagesByCode[primaryImageCode] : null %}
                        {% if primaryImage and primaryImage.thumbnailUrl %}
                            <img src="{{ primaryImage.thumbnailUrl }}" alt="{{ obra.title }}" class="obra-image" />
                        {% else %}
                            <div class="no-image">Sin imagen</div>
                        {% endif %}
                        <div class="obra-title">{{ obra.title ?: 'Sin título' }}</div>
                        <div class="obra-artist">
                            {% if obra.artist %}
                                {{ obra.artist.name }}
                            {% else %}
                                Artista desconocido
                            {% endif %}
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <div style="text-align: center; color: var(--muted); font-style: italic; margin-top: 20pt;">
                No hay obras en esta ubicación
            </div>
        {% endif %}
    </section>
    <footer class="foot">
        <div>
            {% if location.code %}
                <div class="code">Loc. {{ location.code }}</div>
            {% endif %}
            <div class="muted">Chijal, San Cristóbal de las Casas, 2025</div>
        </div>
        <div style="display:flex; align-items:center; gap:10pt;">
            <img src="https://pgsc.survos.com/chijal-landscape.jpg" alt="Chijal" style="height:18pt; width:auto;" />
            <div style="width:54pt;height:54pt;border:1pt solid #000;display:grid;place-items:center;font-size:7pt;color:#555">
                <!-- Faux QR with location icon -->
                <svg width="40" height="40" viewBox="0 0 108 108" xmlns="http://www.w3.org/2000/svg">
                    <rect width="108" height="108" fill="white"/>
                    <rect x="8" y="8" width="24" height="24" fill="#111"/>
                    <rect x="76" y="8" width="24" height="24" fill="#111"/>
                    <rect x="8" y="76" width="24" height="24" fill="#111"/>
                    <!-- Location pin icon -->
                    <path d="M54 32 C60 32 64 36 64 42 C64 50 54 64 54 64 S44 50 44 42 C44 36 48 32 54 32 Z M54 38 C51 38 50 39 50 42 C50 43 51 44 54 44 C57 44 58 43 58 42 C58 39 57 38 54 38 Z" fill="#111"/>
                </svg>
            </div>
        </div>
    </footer>
</main>
</body>
</html>
