<div class="d-flex flex-wrap gap-2">
    {% for obraImage in entity.getInstance().getObraImages() %}
        <img src="{{ asset('images/obra/' ~ obraImage.imageName) }}" alt="Obra Image" width="200" />
    {% endfor %}
</div>
