<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <title>Artist Card 6x4</title>
    <style>
        @page { size: 6in 4in; margin: 0; }
        html, body { height: 100%; }
        html, body, * { box-sizing: border-box; }
        body {
            margin: 0;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
            font-feature-settings: "liga" 1, "kern" 1;
            font-kerning: normal;
        }
        :root {
            --serif: Georgia, "Times New Roman", Times, serif;
            --sans: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            --muted: #555;
            --ink: #111;
        }
        .card-64 {
            width: 6in;
            height: 4in;
            padding: 0.2in;
            color: var(--ink);
            display: grid;
            grid-template-rows: auto 1fr auto;
            gap: 6pt;
            overflow: hidden;
            page-break-after: always;
            border: 1pt solid #000; /* cut guide */
        }
        .title {
            font-family: var(--serif);
            font-weight: 600;
            font-size: 18pt;
            line-height: 1.2;
            hyphens: auto;
            margin: 0;
        }
        .subtitle {
            margin-top: 2pt;
            font-family: var(--sans);
            font-size: 11pt;
            line-height: 1.25;
        }
        .italic { font-style: italic; }
        .muted { color: var(--muted); }
        .artist-header {
            display: flex;
            gap: 10pt;
            align-items: flex-start;
            margin-bottom: 8pt;
        }
        .artist-photo {
            width: 60pt;
            height: 60pt;
            object-fit: cover;
            border: 0.5pt solid #ddd;
            border-radius: 2pt;
            flex-shrink: 0;
        }
        .no-photo {
            width: 60pt;
            height: 60pt;
            background: #f8f8f8;
            border: 1pt dashed #ccc;
            border-radius: 2pt;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 7pt;
            color: var(--muted);
            flex-shrink: 0;
        }
        .artist-info {
            flex: 1;
        }
        .artist-meta {
            font-family: var(--sans);
            font-size: 10pt;
            line-height: 1.3;
            margin-top: 4pt;
        }
        .artist-meta .row { display: block; break-inside: avoid; }
        .artist-meta .label { display: inline-block; min-width: 70pt; color: var(--muted); }
        .obras-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(100pt, 1fr));
            gap: 6pt;
            margin-top: 6pt;
        }
        .obra-item {
            break-inside: avoid;
            page-break-inside: avoid;
        }
        .obra-image {
            width: 100%;
            height: 60pt;
            object-fit: cover;
            border: 0.5pt solid #ddd;
            border-radius: 2pt;
            margin-bottom: 3pt;
        }
        .obra-title {
            font-size: 8pt;
            font-weight: 600;
            line-height: 1.2;
            margin-bottom: 1pt;
        }
        .no-image {
            width: 100%;
            height: 60pt;
            background: #f8f8f8;
            border: 1pt dashed #ccc;
            border-radius: 2pt;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 7pt;
            color: var(--muted);
            margin-bottom: 3pt;
        }
        .foot {
            margin-top: 6pt;
            padding-top: 6pt;
            border-top: 0.5pt solid #bbb;
            font-family: var(--sans);
            font-size: 9pt;
            display: flex;
            gap: 8pt;
            align-items: center;
            justify-content: space-between;
        }
        .code { letter-spacing: 0.2pt; font-variant-numeric: tabular-nums; }
        @media print { body { margin: 0; } }
    </style>
</head>
<body>
<main class="card-64">
    <section>
        <div class="artist-header">
            {% set primaryImageCode = artist.primaryImageCode %}
            {% set primaryImage = primaryImageCode ? imagesByCode[primaryImageCode] : null %}
            {% if primaryImage and primaryImage.thumbnailUrl %}
                <img src="{{ primaryImage.thumbnailUrl }}" alt="{{ artist.name }}" class="artist-photo" />
            {% else %}
                <div class="no-photo">Sin foto</div>
            {% endif %}
            <div class="artist-info">
                {% if artist.name %}
                    <h1 class="title">{{ artist.name }}</h1>
                {% endif %}
                <div class="subtitle">
                    <strong>Artista</strong>
                    {% if artist.birthYear %} · {{ artist.birthYear }}{% endif %}
                </div>
                <div class="artist-meta">
                    {% if obras|length %}
                        <span class="row"><span class="label">Obras</span> {{ obras|length }} obra{{ obras|length != 1 ? 's' : '' }}</span>
                    {% endif %}
                    {% if artist.bio %}
                        <span class="row"><span class="label">Bio</span> {{ artist.bio|u.truncate(60, '...') }}</span>
                    {% endif %}
                </div>
            </div>
        </div>
    </section>
    <section class="obras-content">
        {% if obras|length %}
            <div class="obras-grid">
                {% for obra in obras %}
                    <div class="obra-item">
                        {% set primaryImageCode = obra.primaryImageCode %}
                        {% set primaryImage = primaryImageCode ? imagesByCode[primaryImageCode] : null %}
                        {% if primaryImage and primaryImage.thumbnailUrl %}
                            <img src="{{ primaryImage.thumbnailUrl }}" alt="{{ obra.title }}" class="obra-image" />
                        {% else %}
                            <div class="no-image">Sin imagen</div>
                        {% endif %}
                        <div class="obra-title">{{ obra.title ?: 'Sin título' }}</div>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <div style="text-align: center; color: var(--muted); font-style: italic; margin-top: 20pt;">
                No hay obras registradas para este artista
            </div>
        {% endif %}
    </section>
    <footer class="foot">
        <div>
            {% if artist.code %}
                <div class="code">Art. {{ artist.code }}</div>
            {% endif %}
            <div class="muted">Chijal, San Cristóbal de las Casas, 2025</div>
        </div>
        <div style="display:flex; align-items:center; gap:10pt;">
            <img src="https://pgsc.survos.com/chijal-landscape.jpg" alt="Chijal" style="height:18pt; width:auto;" />
            {% set qrUrl = voxitourEndpoint ~ app.request.locale ~ '/chijal?artistId=' ~ artist.id ~ '#tab-artists' %}
            <img src="{{ qr_code_url(qrUrl) }}" alt="QR Code" style="width:54pt;height:54pt;border:1pt solid #000;background:white;" />
            </div>
        </div>
    </footer>
</main>
</body>
</html>
